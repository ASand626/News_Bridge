import { TermsPageClient } from "@/features/terms/components/TermsPageClient";

export const metadata = { title: "用語集 — News Bridge" };

export default function TermsPage() {
  return <TermsPageClient />;
}
